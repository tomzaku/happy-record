import React, { useRef, useState } from 'react';
// import mySound from './test.mp3';

const AudioPlayer = () => {
  return null;
  // const audioRef = useRef(null); // Create a reference for the audio element
  // const [play, setPlay] = useState(false); // Track whether the audio is playing
  //
  // const togglePlay = () => {
  //   const audio = audioRef.current; // Access the audio element through the ref
  //   if (audio) {
  //     if (play) {
  //       audio.pause(); // Pause the audio if playing
  //     } else {
  //       audio.play(); // Play the audio if paused
  //     }
  //     setPlay(!play); // Toggle the play state
  //   }
  // };
  //
  // return (
  //   <>
  //     <button onClick={togglePlay}>
  //       {play ? 'Pause' : 'Play'} {/* Change button text based on play state */}
  //     </button>
  //     <audio ref={audioRef} src={mySound} />{' '}
  //     {/* Attach the ref to the audio element */}
  //   </>
  // );
};

export default AudioPlayer;

