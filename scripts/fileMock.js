// Jest stub for static asset imports (images, audio, fonts) — see
// moduleNameMapper in jest.config.js. The real content never matters in a
// test, just that `import x from './foo.svg'` resolves to something.
module.exports = 'test-file-stub';
