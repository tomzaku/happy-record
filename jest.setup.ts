// Runs after the test framework is installed in the jsdom environment (see
// setupFilesAfterEnv in jest.config.js) — adds the jest-dom matchers
// (toBeInTheDocument, toHaveClass, ...) to every test's `expect`.
import '@testing-library/jest-dom';
